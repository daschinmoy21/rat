<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- host network: every piece (Spark on the host, metastore, hiveserver2)
         reaches the NameNode through the published port on localhost -->
    <property>
        <name>fs.defaultFS</name>
        <value>hdfs://localhost:8020</value>
    </property>
</configuration>
