<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- connect to datanodes via their advertised hostname (localhost) rather
         than the registered container IP -->
    <property>
        <name>dfs.client.use.datanode.hostname</name>
        <value>true</value>
    </property>
</configuration>
